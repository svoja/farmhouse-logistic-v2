# API Server (use 3001 to avoid conflict with other apps on 3000)
PORT=3001

# Database (MySQL/MariaDB - bread_logistics)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bread_logistics

# Frontend API URL (used by React in production - defaults to same origin)
VITE_API_URL=

# OpenClaw (for AI chatbot)
OPENCLAW_URL=http://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=
