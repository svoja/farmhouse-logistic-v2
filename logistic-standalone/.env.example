# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=logistic_standalone
DB_PORT=3306

# Server
PORT=3002

# Optional: API base URL for client (defaults to same origin /api)
# VITE_API_URL=
